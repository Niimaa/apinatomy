(function (root, factory) {
  if (typeof define === 'function' && define.amd) {
    //     define(['jquery', 'jquery-ui'], factory);
  } else {
    // Browser globals
    root.amyCircuitboard = factory(root.$);
  }
}(this, function($) {
